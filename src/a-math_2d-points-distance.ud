(section
  (ref "a_math_2d_points_distance")
  (title "Math_2D.Points.Distance")
  (para function_desc "Calculate distance between points.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis (render "math_2d-points-distance.txt")))
  (subsection
    (title "Description")
    (para
      "The " (item ada_function "Distance") " function returns the distance
      between " (item parameter "Point_A") " and " (item parameter "Point_B")
      ".")))
