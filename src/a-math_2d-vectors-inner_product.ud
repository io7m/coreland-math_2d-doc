(section
  (ref "a_math_2d_vectors_inner_product")
  (title "Math_2D.Vectors.Inner_Product")
  (para function_desc "Calculate inner product of two vectors.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis (render "math_2d-vectors-inner_product.txt")))
  (subsection
    (title "Description")
    (para
      "The " (item ada_function "Inner_Product") " function returns the inner
      product of the vectors " (item parameter "Vector_A") " and "
      (item parameter "Vector_B") ".")
    (para "The function is equivalent to "
      (link "a_math_2d_vectors_scalar_product" "Scalar_Product") ".")))
