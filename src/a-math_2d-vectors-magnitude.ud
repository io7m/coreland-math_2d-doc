(section
  (ref "a_math_2d_vectors_magnitude")
  (title "Math_2D.Vectors.Magnitude")
  (para function_desc "Calculate magnitude of vector.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis (render "math_2d-vectors-magnitude.txt")))
  (subsection
    (title "Description")
    (para
      "The " (item ada_function "Magnitude") " function calculates the
      magnitude of " (item parameter "Vector") ".")))
