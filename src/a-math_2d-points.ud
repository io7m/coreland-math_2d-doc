(section
  (ref "a_math_2d_points")
  (title "Math_2D.Points")
  (contents)

  (para "The " (item ada_package "Math_2D.Points") " package provides
    subprograms for processing points.")

  (subsection
    (title "Generic Formal Parameters")
    (para-verbatim ada_type_synopsis (render "math_2d-points-generic.txt")))

  (include "a-math_2d-points-distance.ud"))
