(section
  (ref "a_math_2d_vectors_normalize")
  (title "Math_2D.Vectors.Normalize")
  (para subprogram_desc "Normalize vector.")
  (subsection
    (title "Synopsis")
    (para-verbatim subprogram_synopsis (render "math_2d-vectors-normalize.txt")))
  (subsection
    (title "Description")
    (para
      "The " (item ada_subprogram "Normalize") " subprograms normalize "
      (item parameter "Vector") ".")))
